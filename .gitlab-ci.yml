stages:
  - build

build image:
  image: docker:latest
  stage: build
  services:
    - docker:dind
  script:
    - apk add --no-cache docker-compose
    - docker-compose up -d
    - docker build .
    - docker-compose down